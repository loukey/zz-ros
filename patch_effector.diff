diff --git a/src/gui_new/presentation/components/effector/effector_components.py b/src/gui_new/presentation/components/effector/effector_components.py
--- a/src/gui_new/presentation/components/effector/effector_components.py
+++ b/src/gui_new/presentation/components/effector/effector_components.py
@@
-from ..base_component import BaseComponent, default_font, LabeledComboBox
+from ..base_component import BaseComponent, default_font, LabeledComboBox
+try:
+    from shared.config.service_registry import get_serial_view_model
+except ImportError:
+    get_serial_view_model = None
@@
-    def connect_signals(self):
-        """连接视图模型信号"""
-        if self.view_model:
-            self.view_model.connection_status_changed.connect(self.update_connection_status)
+    def connect_signals(self):
+        """连接视图模型信号"""
+        if self.view_model:
+            self.view_model.connection_status_changed.connect(self.update_connection_status)
+            return
+        if get_serial_view_model:
+            try:
+                serial_vm = get_serial_view_model()
+            except Exception:
+                serial_vm = None
+            if serial_vm:
+                self._serial_vm = serial_vm
+                serial_vm.connection_status_changed.connect(self.update_connection_status)
+                try:
+                    self.update_connection_status(serial_vm.get_connection_status())
+                except Exception:
+                    pass
